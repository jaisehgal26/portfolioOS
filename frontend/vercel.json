{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd .. && pnpm install && pip install -r frontend/requirements.txt",
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "ignoreCommand": "cd .. && git diff HEAD^ HEAD --quiet -- frontend/ backend/ package.json pnpm-lock.yaml pnpm-workspace.yaml",
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "/api" },
    { "source": "/health", "destination": "/api" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  }
}
