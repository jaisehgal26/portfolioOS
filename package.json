{
  "name": "jaios-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "JaiOS — modular monorepo (Next.js shell + packages)",
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
